import { CircleLoader } from "react-spinners";

const DefaultLoader = () => {
  return (
    <div className="mt-20">
      <CircleLoader size={100} />
    </div>
  );
};

export default DefaultLoader;
